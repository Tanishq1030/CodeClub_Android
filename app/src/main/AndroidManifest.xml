<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.codeclub.codeclub_android">  <!-- THIS IS YOUR PACKAGE NAME -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/CodeClub_Android"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CodeClub_Android"
        tools:targetApi="31">
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DashboardActivity"
            android:exported="false"/>

        <activity android:name=".SignOptionActivity"
            android:exported="false"/>

        <activity android:name=".MemberRegActivity"
            android:exported="false"/>

        <activity android:name=".AlumniRegActivity"
            android:exported="false"/>

        <activity android:name=".StudentRegActivity"
            android:exported="false"/>
    </application>

</manifest>